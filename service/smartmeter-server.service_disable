[Unit]
Description=Smartmeter Server
After=network.target
StartLimitIntervalSec=0

[Service]

Restart=always
RestartSec=1
Type=simple
User=beerm
ExecStart=root="/home/beerm/projects/utilities" && bash ${root}/start-server.sh -r $root

[Install]
WantedBy=multi-user.target
